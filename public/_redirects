# Cloudflare Pages redirects. Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
# Some pre-existing article bodies link to individual articles using the
# plural listing prefix (/articles/<YYYY-MM>/<slug>) instead of the correct
# singular article route (/article/<YYYY-MM>/<slug>). The plural prefix is
# also used, on its own, by the real paginated article-listing route
# (/articles/2, /articles/3, ...), which has only ONE path segment after
# /articles/. This rule requires exactly TWO segments (:year/:slug), so it
# cannot match the single-segment pagination URLs and leaves them untouched.
/articles/:year/:slug  /article/:year/:slug  301

# The "GitHub Ships an Actions Network Firewall" article (2026-08-11, PR
# #2170) cross-references two prior articles but dropped their day-of-month
# slug prefix. The article body is Ed25519-signed and immutable, so the fix
# lives here rather than in the signed content. Real destinations verified
# to exist in src/content/articles/2026-07/.
/article/2026-07/github-makes-a-three-day-package-cooldown-the-default-for-dependabot-version-updates-citing-supply-chain-attacks-through-new-releases  /article/2026-07/20-github-makes-a-three-day-package-cooldown-the-default-for-dependabot-version-updates-citing-supply-chain-attacks-through-new-releases  301
/article/2026-07/npm-v12-ships-with-install-scripts-disabled-by-default-after-a-year-of-supply-chain-attacks  /article/2026-07/13-npm-v12-ships-with-install-scripts-disabled-by-default-after-a-year-of-supply-chain-attacks  301
