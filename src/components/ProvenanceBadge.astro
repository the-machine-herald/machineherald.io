---
import type { ProvenanceData } from '@/lib/provenance';

interface Props {
  provenance: ProvenanceData | null;
  slug: string;
  sourcesCount: number;
}

const { provenance, slug, sourcesCount } = Astro.props;

const hasProvenance = !!provenance;
const hasPublisherSignature = provenance?.signatures_present?.publisher ?? false;
const hasContributorSignature = provenance?.signatures_present?.contributor ?? false;
const articleHashShort = provenance?.article_sha256
  ? provenance.article_sha256.replace(/^sha256:/, '').slice(0, 10)
  : null;
---

<div class="provenance-badge">
  {hasProvenance ? (
    <>
      <div class="flex items-center gap-1.5">
        <svg
          class="w-4 h-4 text-emerald-600 dark:text-emerald-400"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          stroke-width="2"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-emerald-700 dark:text-emerald-400">Verified pipeline</span>
      </div>
      <span class="text-border-light dark:text-border-dark" aria-hidden="true">|</span>
      <span>Sources: {sourcesCount}</span>
      <span class="text-border-light dark:text-border-dark" aria-hidden="true">|</span>
      <span>
        Publisher: {hasPublisherSignature ? (
          <span class="text-emerald-700 dark:text-emerald-400">signed</span>
        ) : (
          <span class="text-amber-600 dark:text-amber-400">pending</span>
        )}
      </span>
      <span class="text-border-light dark:text-border-dark" aria-hidden="true">|</span>
      <span>
        Contributor: {hasContributorSignature ? (
          <span class="text-emerald-700 dark:text-emerald-400">signed</span>
        ) : (
          <span class="text-zinc-600 dark:text-zinc-400">—</span>
        )}
      </span>
      {articleHashShort && (
        <>
          <span class="text-border-light dark:text-border-dark" aria-hidden="true">|</span>
          <span class="font-mono text-xs text-text-muted-light dark:text-text-muted-dark">
            Hash: {articleHashShort}
          </span>
        </>
      )}
      <span class="text-border-light dark:text-border-dark" aria-hidden="true">|</span>
      <a
        href={`/provenance/${slug}`}
        class="text-accent hover:underline"
      >
        View
      </a>
    </>
  ) : (
    <>
      <div class="flex items-center gap-1.5">
        <svg
          class="w-4 h-4 text-amber-600 dark:text-amber-400"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          stroke-width="2"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
        </svg>
        <span class="text-amber-700 dark:text-amber-400">Provenance unavailable</span>
      </div>
      <span class="text-border-light dark:text-border-dark" aria-hidden="true">|</span>
      <span>Sources: {sourcesCount}</span>
    </>
  )}
</div>
